# 计算机网络实验报告

## 实验二：IP 和 TCP 数据分组的捕获和解析

------

## 一、实验基本信息

| 项目       | 内容                           |
| ---------- | ------------------------------ |
| 实验名称   | IP 和 TCP 数据分组的捕获和解析 |
| 实验类别   | 协议分析型                     |
| 实验学时   | 4 学时                         |
| 实验组人数 | 单人 1 组                      |
| 学生姓名   | 杨龙驹                         |
| 学号       | 2024211139                     |
| 班级       | 2024211308                     |
| 实验日期   | 2026.6.1                       |
| 实验地点   | 本地电脑                       |

------

## 二、实验目的

本次实验的主要目的是通过 Wireshark 抓包工具，观察并分析主机连接 Internet 过程中产生的典型网络报文，理解常见网络协议的作用和报文结构。

具体包括：

1. 掌握 Wireshark 的基本使用方法，包括选择网卡、设置过滤器、捕获和查看报文。
2. 捕获并分析 DHCP 报文，理解主机自动获取 IP 地址、默认网关、DNS 等网络参数的过程。
3. 捕获并分析 ARP 报文，理解 IP 地址到 MAC 地址的解析过程。
4. 捕获并分析 ICMP 报文，理解 ping 命令的工作过程。
5. 捕获并分析 IP 数据分组，理解 IPv4 报文的基本格式。
6. 分析长度大于 1500 字节的 IP 数据分组的分片传输过程。
7. 捕获并分析 TCP 连接建立、数据通信和连接拆除的流程。

------

## 三、实验环境

| 项目         | 内容                                             |
| ------------ | ------------------------------------------------ |
| 操作系统     | Windows 【填写版本，如 Windows 10 / Windows 11】 |
| 抓包软件     | Wireshark-4.6.6                                  |
| 网络连接方式 | Wi-Fi（校园网）                                  |
| 本机 IP 地址 | 【实验中填写】                                   |
| 子网掩码     | 【实验中填写】                                   |
| 默认网关     | 【实验中填写】                                   |
| DNS 服务器   | 【实验中填写】                                   |
| 使用网卡     | 【实验中填写，例如 WLAN / Ethernet】             |

------

## 四、实验原理简述

### 4.1 DHCP 协议原理

DHCP，即动态主机配置协议，主要用于主机自动获取网络配置参数，包括 IP 地址、子网掩码、默认网关、DNS 服务器等。

DHCP 获取地址的典型过程包括四个阶段：

```
DHCP Discover
DHCP Offer
DHCP Request
DHCP ACK
```

其作用分别为：

| 阶段          | 作用                                      |
| ------------- | ----------------------------------------- |
| DHCP Discover | 客户端广播寻找 DHCP 服务器                |
| DHCP Offer    | DHCP 服务器向客户端提供可用 IP 地址       |
| DHCP Request  | 客户端请求使用服务器提供的 IP 地址        |
| DHCP ACK      | DHCP 服务器确认分配 IP 地址及相关网络参数 |

------

### 4.2 ARP 协议原理

ARP，即地址解析协议，用于根据 IP 地址查询对应的 MAC 地址。

当主机需要向同一局域网中的某个目标发送数据，或者需要将数据发送给默认网关时，需要先通过 ARP 获取目标设备或网关的 MAC 地址。

ARP 的基本过程为：

```
ARP Request：谁拥有某个 IP 地址？请告诉我。
ARP Reply：这个 IP 地址对应的 MAC 地址是某某。
```

------

### 4.3 ICMP 与 ping 命令原理

ping 命令通常使用 ICMP 协议来测试网络连通性。

ping 的基本过程包括：

```
ICMP Echo Request
ICMP Echo Reply
```

其中，Echo Request 表示请求报文，Echo Reply 表示响应报文。

------

### 4.4 IP 分片原理

在以太网中，常见 MTU 为 1500 字节。当一个 IPv4 数据报长度超过链路层允许的最大传输单元时，就需要进行 IP 分片。

IP 分片中需要重点观察以下字段：

| 字段            | 含义                                         |
| --------------- | -------------------------------------------- |
| Identification  | 标识同一个原始 IP 数据报的各个分片           |
| Flags           | 标志字段，其中 MF 表示后面是否还有更多分片   |
| Fragment Offset | 分片偏移量，表示当前分片在原始数据报中的位置 |
| Total Length    | 当前 IP 分片的总长度                         |
| Protocol        | 上层协议类型                                 |

------

### 4.5 TCP 协议原理

TCP 是面向连接的可靠传输协议。一次完整的 TCP 通信通常包括：

1. 建立连接，即三次握手；
2. 数据传输；
3. 释放连接，即四次挥手或优雅拆除连接。

TCP 三次握手过程为：

```
客户端 -> 服务器：SYN
服务器 -> 客户端：SYN, ACK
客户端 -> 服务器：ACK
```

TCP 连接释放过程通常为：

```
一方 -> 另一方：FIN, ACK
另一方 -> 一方：ACK
另一方 -> 一方：FIN, ACK
一方 -> 另一方：ACK
```

------

## 五、实验步骤

### 5.1 Wireshark 准备工作

1. 启动计算机并连接网络，确保能够正常访问 Internet。
2. 打开 Wireshark 软件。
3. 在 Wireshark 中选择当前正在使用的网卡。
4. 点击 Start 开始抓包。
5. 根据实验需要设置捕获过滤器或显示过滤器。

实验截图：

> 图 1：Wireshark 主界面及网卡选择截图
>  【此处插入截图】

------

### 5.2 捕获 DHCP 报文

#### 5.2.1 实验操作

1. 打开 Wireshark，选择当前使用的网卡并开始抓包。
2. 设置过滤器，观察 DHCP 相关报文。

显示过滤器可以使用：

```
udp.port == 67 || udp.port == 68
```

或：

```
bootp
```

1. 打开 Windows 命令行窗口，执行以下命令：

```
ipconfig /release
ipconfig /renew
```

1. 在 Wireshark 中观察 DHCP 获取 IP 地址的过程。

#### 5.2.2 实验截图

> 图 2：DHCP 报文捕获结果
>  【此处插入截图】

> 图 3：DHCP 报文详细字段展开截图
>  【此处插入截图】

#### 5.2.3 DHCP 报文分析表

| 序号 | 报文类型      | 源 IP    | 目的 IP  | 源端口 | 目的端口 | 作用                   |
| ---- | ------------- | -------- | -------- | ------ | -------- | ---------------------- |
| 1    | DHCP Discover | 【填写】 | 【填写】 | 68     | 67       | 客户端寻找 DHCP 服务器 |
| 2    | DHCP Offer    | 【填写】 | 【填写】 | 67     | 68       | 服务器提供 IP 地址     |
| 3    | DHCP Request  | 【填写】 | 【填写】 | 68     | 67       | 客户端请求使用该 IP    |
| 4    | DHCP ACK      | 【填写】 | 【填写】 | 67     | 68       | 服务器确认分配 IP      |

#### 5.2.4 DHCP 获取到的网络参数

| 参数            | 值       |
| --------------- | -------- |
| 分配的 IP 地址  | 【填写】 |
| 子网掩码        | 【填写】 |
| 默认网关        | 【填写】 |
| DNS 服务器      | 【填写】 |
| DHCP 服务器地址 | 【填写】 |
| 租约时间        | 【填写】 |

#### 5.2.5 小结

通过 DHCP 报文分析可以看出，主机在接入网络时并不是直接拥有 IP 地址，而是需要通过 DHCP 服务器动态获取 IP 地址、默认网关、DNS 等网络参数。DHCP 的四次交互保证了地址分配过程的完整性和可靠性。

------

### 5.3 捕获 ARP 和 ICMP 报文

#### 5.3.1 实验操作

1. 重新设置 Wireshark 捕获选项和显示过滤器。
2. 在命令行中执行 ping 命令，例如：

```
ping www.bupt.edu.cn
```

或：

```
ping 8.8.8.8
```

1. 在 Wireshark 中观察 ARP 和 ICMP 报文。

可使用显示过滤器：

```
arp
```

或：

```
icmp
```

也可以使用：

```
arp || icmp
```

#### 5.3.2 实验截图

> 图 4：ARP 请求和响应报文截图
>  【此处插入截图】

> 图 5：ICMP Echo Request 和 Echo Reply 报文截图
>  【此处插入截图】

#### 5.3.3 ARP 报文分析表

| 序号 | 报文类型    | 源 MAC   | 目的 MAC  | 源 IP    | 目的 IP  | 作用                        |
| ---- | ----------- | -------- | --------- | -------- | -------- | --------------------------- |
| 1    | ARP Request | 【填写】 | Broadcast | 【填写】 | 【填写】 | 查询目标 IP 对应的 MAC 地址 |
| 2    | ARP Reply   | 【填写】 | 【填写】  | 【填写】 | 【填写】 | 返回目标 IP 对应的 MAC 地址 |

#### 5.3.4 ICMP 报文分析表

| 序号 | 报文类型     | 源 IP    | 目的 IP  | Type | Code | 作用           |
| ---- | ------------ | -------- | -------- | ---- | ---- | -------------- |
| 1    | Echo Request | 【填写】 | 【填写】 | 8    | 0    | 发送 ping 请求 |
| 2    | Echo Reply   | 【填写】 | 【填写】 | 0    | 0    | 返回 ping 响应 |

#### 5.3.5 小结

通过抓包可以看到，在执行 ping 命令之前，主机可能会先通过 ARP 获取目标主机或默认网关的 MAC 地址。随后，主机发送 ICMP Echo Request 报文，对方收到后返回 ICMP Echo Reply 报文，从而完成连通性测试。

------

### 5.4 分析 IP 数据分组的分片传输过程

#### 5.4.1 实验操作

根据实验指导书要求，使用 Windows ping 命令的 `-l` 选项构造大于 8000 字节的 IP 数据分组，例如：

```
ping -l 8000 www.bupt.edu.cn
```

若在校外访问北邮网站，可能需要连接北邮 VPN。

Wireshark 中可以使用如下显示过滤器观察 IP 分片：

```
ip.flags.mf == 1 || ip.frag_offset > 0
```

也可以结合 ICMP 过滤：

```
icmp || ip.flags.mf == 1 || ip.frag_offset > 0
```

#### 5.4.2 实验截图

> 图 6：大长度 ping 命令执行结果
>  【此处插入截图】

> 图 7：IP 分片报文列表截图
>  【此处插入截图】

> 图 8：某个 IP 分片详细字段展开截图
>  【此处插入截图】

#### 5.4.3 IP 分片分析表

| 分片序号 | Identification | Total Length | MF 标志  | Fragment Offset | 说明         |
| -------- | -------------- | ------------ | -------- | --------------- | ------------ |
| 1        | 【填写】       | 【填写】     | 【填写】 | 【填写】        | 第一个分片   |
| 2        | 【填写】       | 【填写】     | 【填写】 | 【填写】        | 中间分片     |
| 3        | 【填写】       | 【填写】     | 【填写】 | 【填写】        | 中间分片     |
| ...      | 【填写】       | 【填写】     | 【填写】 | 【填写】        | 中间分片     |
| 最后一片 | 【填写】       | 【填写】     | 0        | 【填写】        | 最后一个分片 |

#### 5.4.4 分片字段分析

本次实验中，构造了长度大于 8000 字节的 IP 数据分组。由于该数据分组长度超过了常见以太网 MTU 1500 字节，因此在传输过程中产生了 IP 分片。

从抓包结果可以观察到：

1. 同一个原始 IP 数据报的所有分片具有相同的 Identification 字段。
2. 除最后一个分片外，其他分片的 MF 标志位为 1，表示后面还有更多分片。
3. 最后一个分片的 MF 标志位为 0，表示该分片是最后一个分片。
4. 不同分片的 Fragment Offset 字段逐渐增大，用于表示当前分片在原始 IP 数据报中的位置。
5. 接收端可以根据源 IP、目的 IP、协议号、Identification 和 Fragment Offset 等信息将多个分片重新组装为原始 IP 数据报。

#### 5.4.5 小结

IP 分片机制解决了较大 IP 数据报无法一次性通过链路层传输的问题。通过本实验可以直观观察到 IP 数据报被拆分成多个分片的过程，并理解 Identification、MF 标志和 Fragment Offset 字段在分片重组中的作用。

------

### 5.5 分析 TCP 通信过程

#### 5.5.1 实验操作

1. 打开 Wireshark，开始抓包。
2. 访问一个网页，或使用其他能够产生 TCP 连接的网络应用。
3. 使用过滤器查看 TCP 报文：

```
tcp
```

1. 选择一条 TCP 连接，使用如下过滤器只查看该连接：

```
tcp.stream eq 【填写编号】
```

1. 观察 TCP 三次握手、数据传输和连接拆除过程。

#### 5.5.2 实验截图

> 图 9：TCP 三次握手报文截图
>  【此处插入截图】

> 图 10：TCP 数据传输过程截图
>  【此处插入截图】

> 图 11：TCP 连接拆除过程截图
>  【此处插入截图】

#### 5.5.3 TCP 三次握手分析表

| 序号 | 源 IP:端口 | 目的 IP:端口 | 标志位   | Seq      | Ack      | 作用                     |
| ---- | ---------- | ------------ | -------- | -------- | -------- | ------------------------ |
| 1    | 【填写】   | 【填写】     | SYN      | 【填写】 | 【填写】 | 客户端请求建立连接       |
| 2    | 【填写】   | 【填写】     | SYN, ACK | 【填写】 | 【填写】 | 服务器确认并请求建立连接 |
| 3    | 【填写】   | 【填写】     | ACK      | 【填写】 | 【填写】 | 客户端确认，连接建立     |

#### 5.5.4 TCP 数据通信分析表

| 序号 | 源 IP:端口 | 目的 IP:端口 | 标志位         | Seq      | Ack      | Len      | 说明           |
| ---- | ---------- | ------------ | -------------- | -------- | -------- | -------- | -------------- |
| 1    | 【填写】   | 【填写】     | ACK / PSH, ACK | 【填写】 | 【填写】 | 【填写】 | 客户端发送数据 |
| 2    | 【填写】   | 【填写】     | ACK            | 【填写】 | 【填写】 | 【填写】 | 服务器确认     |
| 3    | 【填写】   | 【填写】     | ACK / PSH, ACK | 【填写】 | 【填写】 | 【填写】 | 服务器返回数据 |
| 4    | 【填写】   | 【填写】     | ACK            | 【填写】 | 【填写】 | 【填写】 | 客户端确认     |

#### 5.5.5 TCP 连接拆除分析表

| 序号 | 源 IP:端口 | 目的 IP:端口 | 标志位   | Seq      | Ack      | 作用               |
| ---- | ---------- | ------------ | -------- | -------- | -------- | ------------------ |
| 1    | 【填写】   | 【填写】     | FIN, ACK | 【填写】 | 【填写】 | 一方请求关闭连接   |
| 2    | 【填写】   | 【填写】     | ACK      | 【填写】 | 【填写】 | 对方确认关闭请求   |
| 3    | 【填写】   | 【填写】     | FIN, ACK | 【填写】 | 【填写】 | 对方也请求关闭连接 |
| 4    | 【填写】   | 【填写】     | ACK      | 【填写】 | 【填写】 | 最终确认，连接释放 |

#### 5.5.6 小结

通过 TCP 报文分析可以看到，TCP 在正式传输数据之前需要通过三次握手建立连接。数据传输过程中，双方通过 Sequence Number 和 Acknowledgment Number 保证数据的有序传输和可靠确认。通信结束后，TCP 通过 FIN 和 ACK 报文完成连接的优雅拆除。

------

## 六、实验结果汇总

本实验共观察并分析了以下类型的报文：

| 协议    | 是否捕获到 | 主要作用                      | 备注     |
| ------- | ---------- | ----------------------------- | -------- |
| DHCP    | 是 / 否    | 自动获取 IP、网关、DNS 等参数 | 【填写】 |
| ARP     | 是 / 否    | 根据 IP 查询 MAC 地址         | 【填写】 |
| ICMP    | 是 / 否    | ping 连通性测试               | 【填写】 |
| IP 分片 | 是 / 否    | 传输大于 MTU 的 IP 数据报     | 【填写】 |
| TCP     | 是 / 否    | 建立连接、传输数据、拆除连接  | 【填写】 |

------

## 七、实验中遇到的问题及解决方法

### 问题 1：未能捕获到 DHCP 报文

原因分析：

【填写，例如：主机已经获得 IP，没有重新申请；过滤器设置错误；选错网卡等。】

解决方法：

【填写，例如：重新执行 `ipconfig /release` 和 `ipconfig /renew`，重新选择正确网卡，修改显示过滤器等。】

------

### 问题 2：ping 命令无法收到响应

原因分析：

【填写，例如：目标服务器禁止 ICMP、网络不通、VPN 未连接等。】

解决方法：

【填写，例如：更换目标地址，检查网络连接，连接 VPN 等。】

------

### 问题 3：未能观察到 IP 分片

原因分析：

【填写，例如：过滤器设置错误、目标不可达、系统或网络环境未产生 IPv4 分片等。】

解决方法：

【填写，例如：使用 `ip.flags.mf == 1 || ip.frag_offset > 0` 过滤器，调整 ping 包大小，确认使用 IPv4。】

------

### 问题 4：TCP 拆除连接过程不完整

原因分析：

【填写，例如：抓包时间过短、连接未正常关闭、网页使用长连接等。】

解决方法：

【填写，例如：延长抓包时间，关闭网页或应用，选择完整的 TCP stream 分析。】

------

## 八、实验总结

通过本次实验，我掌握了 Wireshark 抓包工具的基本使用方法，并通过实际抓包观察了 DHCP、ARP、ICMP、IP 分片和 TCP 等协议的工作过程。

在 DHCP 分析中，我理解了主机自动获取 IP 地址、默认网关和 DNS 服务器的过程；在 ARP 和 ICMP 分析中，我理解了局域网地址解析和 ping 连通性测试的基本原理；在 IP 分片分析中，我观察到了大于 MTU 的 IP 数据报被拆分为多个分片进行传输的过程；在 TCP 分析中，我进一步理解了 TCP 三次握手、数据传输和连接拆除的完整流程。

本实验将课堂中学习的协议理论与真实网络报文结合起来，加深了我对计算机网络分层结构、网络层协议和传输层协议工作机制的理解。

------

## 九、附录：实验中使用的主要命令和过滤器

### 9.1 Windows 命令

```
ipconfig /release
ipconfig /renew
ping www.bupt.edu.cn
ping -l 8000 www.bupt.edu.cn
```

### 9.2 Wireshark 常用显示过滤器

| 分析内容        | 显示过滤器           |
| --------------- | -------------------- |
| DHCP            | `udp.port == 67      |
| DHCP            | `bootp`              |
| ARP             | `arp`                |
| ICMP            | `icmp`               |
| ARP 和 ICMP     | `arp                 |
| IP 分片         | `ip.flags.mf == 1    |
| TCP             | `tcp`                |
| TCP SYN 报文    | `tcp.flags.syn == 1` |
| TCP FIN 报文    | `tcp.flags.fin == 1` |
| 某一条 TCP 连接 | `tcp.stream eq 0`    |

------

## 十、截图清单

| 图号  | 截图内容                       | 是否已插入 |
| ----- | ------------------------------ | ---------- |
| 图 1  | Wireshark 选择网卡界面         | 是 / 否    |
| 图 2  | DHCP 四次交互报文列表          | 是 / 否    |
| 图 3  | DHCP 报文详细字段              | 是 / 否    |
| 图 4  | ARP Request / Reply 报文       | 是 / 否    |
| 图 5  | ICMP Echo Request / Reply 报文 | 是 / 否    |
| 图 6  | `ping -l 8000` 命令执行结果    | 是 / 否    |
| 图 7  | IP 分片报文列表                | 是 / 否    |
| 图 8  | IP 分片字段详情                | 是 / 否    |
| 图 9  | TCP 三次握手                   | 是 / 否    |
| 图 10 | TCP 数据传输                   | 是 / 否    |
| 图 11 | TCP 连接拆除                   | 是 / 否    |