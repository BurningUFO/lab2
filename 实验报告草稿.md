# 计算机网络实验二：IP 和 TCP 数据分组的捕获和解析

## 一、实验基本信息

| 项目       | 内容                           |
| ---------- | ------------------------------ |
| 实验名称   | IP 和 TCP 数据分组的捕获和解析 |
| 实验类别   | 协议分析型                     |
| 实验学时   | 4 学时                         |
| 实验组人数 | 单人 1 组                      |
| 学生姓名   | 杨龙驹                         |
| 学号       | 2024211139                     |
| 班级       | 2024211308                     |
| 实验日期   | 2026.6.1                       |
| 实验地点   | 本地电脑                       |

------

## 二、实验目的

本次实验的主要目的是通过 Wireshark 抓包工具，观察并分析主机连接 Internet 过程中产生的典型网络报文，理解常见网络协议的作用和报文结构。

具体包括：

1. 掌握 Wireshark 的基本使用方法，包括选择网卡、设置过滤器、捕获和查看报文。
2. 捕获并分析 DHCP 报文，理解主机自动获取 IP 地址、默认网关、DNS 等网络参数的过程。
3. 捕获并分析 ARP 报文，理解 IP 地址到 MAC 地址的解析过程。
4. 捕获并分析 ICMP 报文，理解 ping 命令的工作过程。
5. 捕获并分析 IP 数据分组，理解 IPv4 报文的基本格式。
6. 分析长度大于 1500 字节的 IP 数据分组的分片传输过程。
7. 捕获并分析 TCP 连接建立、数据通信和连接拆除的流程。

------

## 三、实验环境

| 项目         | 内容                         |
| ------------ | ---------------------------- |
| 操作系统     | Windows 【待补充具体版本】   |
| 抓包软件     | Wireshark-4.6.6              |
| 网络连接方式 | Wi-Fi（校园网）              |
| 本机 IP 地址 | 10.21.213.119                |
| 子网掩码     | 255.255.128.0                |
| 默认网关     | 10.21.128.1                  |
| DNS 服务器   | 10.3.9.5、10.3.9.4、10.3.9.6 |
| DHCP 服务器  | 10.3.9.2                     |
| 使用网卡     | WLAN                         |
| 网卡型号     | Intel(R) Wi-Fi 6E AX211 160MHz |
| 物理地址     | D4-F3-2D-97-C7-4B            |

![CMD 无线网卡参数](image/CMD无线网卡参数.png)

------

## 五、实验步骤

### 5.1 Wireshark 准备工作

实验开始前，先启动计算机并连接校园网，确认主机能够正常访问 Internet。随后打开 Wireshark，在网卡列表中选择当前正在使用的 WLAN 无线网卡，并点击开始按钮进行抓包。为确认网卡选择正确，先进行了短时间抓包测试，观察到该网卡上存在实时数据包流量，说明抓包接口选择正确。

实验截图：

> 图 1：Wireshark 网卡选择界面



> 图 2：Wireshark 短抓包测试界面



### 5.2 捕获 DHCP 报文并分析

#### 5.2.1 实验操作过程

在本阶段实验中，首先打开 Wireshark，选择当前正在使用的 `WLAN` 无线网卡作为捕获接口。由于 DHCP 协议基于 UDP 传输，因此在开始捕获时设置捕获过滤器为：

```text
udp
```

开始抓包后，在 Wireshark 顶部的显示过滤器中输入：

```text
udp.port == 68
```

该过滤条件用于显示与 DHCP 客户端端口相关的报文。随后打开 Windows 命令提示符，依次执行以下命令：

```bat
ipconfig /release
ipconfig /renew
```

其中，`ipconfig /release` 用于释放当前已经申请到的 IP 地址，`ipconfig /renew` 用于重新向 DHCP 服务器申请 IP 地址。执行上述命令后，在 Wireshark 中成功捕获到了 DHCP Release、DHCP Discover、DHCP Offer、DHCP Request 和 DHCP ACK 报文。

【插入图 5-2-1：DHCP 报文捕获结果】



------

#### 5.2.2 DHCP 报文捕获结果

从 Wireshark 捕获结果可以看到，主机首先向 DHCP 服务器发送 DHCP Release 报文，释放原先使用的 IP 地址。随后，主机重新开始 DHCP 地址申请过程，依次产生 DHCP Discover、DHCP Offer、DHCP Request 和 DHCP ACK 报文。

本次实验中捕获到的 DHCP 报文如下表所示。

| 序号 | 报文类型      | 源 IP 地址      | 目的 IP 地址      | 源端口 | 目的端口 | Transaction ID | 作用                                |
| ---- | ------------- | --------------- | ----------------- | ------ | -------- | -------------- | ----------------------------------- |
| 39   | DHCP Release  | `10.21.213.119` | `10.3.9.2`        | 68     | 67       | `0x54112bdf`   | 客户端释放原先申请到的 IP 地址      |
| 40   | DHCP Discover | `0.0.0.0`       | `255.255.255.255` | 68     | 67       | `0xf18cd434`   | 客户端广播寻找 DHCP 服务器          |
| 41   | DHCP Offer    | `10.3.9.2`      | `10.21.213.119`   | 67     | 68       | `0xf18cd434`   | DHCP 服务器向客户端提供可用 IP 地址 |
| 42   | DHCP Request  | `0.0.0.0`       | `255.255.255.255` | 68     | 67       | `0xf18cd434`   | 客户端请求使用服务器提供的 IP 地址  |
| 43   | DHCP ACK      | `10.3.9.2`      | `10.21.213.119`   | 67     | 68       | `0xf18cd434`   | DHCP 服务器确认分配该 IP 地址       |

由表中可以看出，DHCP Discover、DHCP Offer、DHCP Request 和 DHCP ACK 四个报文的 Transaction ID 均为 `0xf18cd434`，说明它们属于同一次 DHCP 地址申请过程。

------

#### 5.2.3 DHCP 四次交互过程分析

从抓包结果可以看出，第 40 至第 43 个报文构成了 DHCP 获取 IP 地址的完整四次交互过程。

首先，客户端在尚未获得有效 IP 地址时，以 `0.0.0.0` 作为源 IP 地址，向广播地址 `255.255.255.255` 发送 DHCP Discover 报文。该报文的作用是在当前局域网中寻找可用的 DHCP 服务器。

随后，DHCP 服务器 `10.3.9.2` 向客户端返回 DHCP Offer 报文，为客户端提供一个可用的 IP 地址。从抓包结果可以看到，服务器提供给客户端的地址为 `10.21.213.119`。

接着，客户端再次以 `0.0.0.0` 为源地址，向广播地址 `255.255.255.255` 发送 DHCP Request 报文，表示客户端请求使用 DHCP 服务器提供的 IP 地址。

最后，DHCP 服务器 `10.3.9.2` 向客户端返回 DHCP ACK 报文，确认将 IP 地址 `10.21.213.119` 分配给该客户端。至此，客户端完成 DHCP 地址申请过程，并获得可以用于网络通信的 IP 地址。

------

#### 5.2.4 DHCP ACK 报文字段分析

为了进一步分析 DHCP 服务器下发的网络配置参数，选中第 43 帧 `DHCP ACK` 报文，并展开其中的 `Dynamic Host Configuration Protocol (ACK)` 字段。

在 DHCP ACK 报文中，`Your (client) IP address` 字段为 `10.21.213.119`，表示 DHCP 服务器最终分配给本机的 IPv4 地址。除此之外，DHCP ACK 报文的 Options 字段中还包含 DHCP 服务器地址、IP 地址租约时间、子网掩码、默认网关和 DNS 服务器等参数。这些参数共同构成了主机接入网络后正常通信所需要的基本网络配置。

【插入图 5-2-2：DHCP ACK 报文详细字段】



本次实验中 DHCP ACK 报文的关键字段如下表所示。

| 参数名称        | Wireshark 字段                    | 实验中观察到的值        | 字段作用                                            |
| --------------- | --------------------------------- | ----------------------- | --------------------------------------------------- |
| DHCP 报文类型   | Option 53: DHCP Message Type      | DHCP ACK                | 表示该报文是服务器对客户端地址申请的最终确认        |
| Transaction ID  | Transaction ID                    | `0xf18cd434`            | 用于标识同一次 DHCP 交互过程                        |
| 客户端 IP 地址  | Your (client) IP address          | `10.21.213.119`         | DHCP 服务器分配给本机的 IPv4 地址                   |
| DHCP 服务器地址 | Option 54: DHCP Server Identifier | `10.3.9.2`              | 表示为客户端分配地址的 DHCP 服务器                  |
| IP 地址租约时间 | Option 51: IP Address Lease Time  | 1 hour，即 3600 seconds | 表示该 IP 地址的有效使用时间                        |
| 子网掩码        | Option 1: Subnet Mask             | `255.255.128.0`         | 用于划分网络号和主机号                              |
| 默认网关        | Option 3: Router                  | `10.21.128.1`           | 主机访问其他网络时使用的下一跳路由器地址            |
| DNS 服务器 1    | Option 6: Domain Name Server      | `10.3.9.5`              | 用于将域名解析为 IP 地址                            |
| DNS 服务器 2    | Option 6: Domain Name Server      | `10.3.9.4`              | 备用 DNS 服务器                                     |
| DNS 服务器 3    | Option 6: Domain Name Server      | `10.3.9.6`              | 备用 DNS 服务器                                     |
| Relay Agent IP  | Relay agent IP address            | `10.21.128.1`           | DHCP 中继代理地址，用于在不同网络之间转发 DHCP 报文 |
| 客户端 MAC 地址 | Client MAC address                | `d4:f3:2d:97:c7:4b`     | 用于标识发起 DHCP 请求的客户端网卡                  |

------

#### 5.2.5 DHCP 字段结果核对

为了验证 DHCP ACK 报文中下发的网络配置是否被本机成功采用，在 Windows 命令提示符中执行 `ipconfig /renew` 后，查看 WLAN 无线网卡的网络配置。命令行输出结果显示，本机 WLAN 网卡获得的 IPv4 地址为 `10.21.213.119`，子网掩码为 `255.255.128.0`，默认网关为 `10.21.128.1`。

【插入图 5-2-3：ipconfig /renew 后 WLAN 网络配置结果】



将命令行结果与 Wireshark 中 DHCP ACK 报文的字段进行对比，可以得到如下结果。

| 对比项目  | DHCP ACK 报文中的值 | `ipconfig /renew` 后系统显示的值 | 是否一致 |
| --------- | ------------------- | -------------------------------- | -------- |
| IPv4 地址 | `10.21.213.119`     | `10.21.213.119`                  | 一致     |
| 子网掩码  | `255.255.128.0`     | `255.255.128.0`                  | 一致     |
| 默认网关  | `10.21.128.1`       | `10.21.128.1`                    | 一致     |

由此可见，DHCP 服务器下发的网络配置参数已经被 Windows 主机成功接收并应用。

------

#### 5.2.6 本节小结

通过本阶段实验，成功捕获并分析了 DHCP 报文。实验中主机首先发送 DHCP Release 报文释放原有 IP 地址，随后通过 DHCP Discover、DHCP Offer、DHCP Request 和 DHCP ACK 四次交互重新获得 IP 地址。

本次实验中，客户端最终获得的 IP 地址为 `10.21.213.119`，DHCP 服务器地址为 `10.3.9.2`，子网掩码为 `255.255.128.0`，默认网关为 `10.21.128.1`，DNS 服务器为 `10.3.9.5`、`10.3.9.4` 和 `10.3.9.6`。这些信息说明 DHCP 协议不仅能够自动分配 IP 地址，还能够同时为主机配置默认网关、DNS 服务器和租约时间等网络参数，从而使主机能够正常接入网络并访问 Internet。

—

### 5.3 捕获 ARP 和 ICMP 报文并分析

#### 5.3.1 实验操作过程

在完成 DHCP 报文捕获和分析后，重新设置 Wireshark 的捕获选项和显示选项。本阶段实验仍然选择当前正在使用的 `WLAN` 无线网卡作为捕获接口。为了避免遗漏 ARP 和 ICMP 报文，本次抓包不再设置捕获过滤器，开始抓包后在 Wireshark 顶部的显示过滤器中输入：

```text
arp || icmp
```

该过滤器用于同时显示 ARP 报文和 ICMP 报文。

随后在 Windows 命令提示符中执行 ping 命令，对默认网关 `10.21.128.1` 进行连通性测试：

```bat
ping -4 10.21.128.1
```

命令执行结果显示，主机共发送 4 个 ICMP 请求报文，收到 4 个 ICMP 响应报文，丢包率为 0%，说明本机与默认网关之间网络连通正常。

【插入图 5-3-1：ping 命令执行结果】

------

#### 5.3.2 ARP 和 ICMP 报文捕获结果

执行 ping 命令后，在 Wireshark 中使用 `arp || icmp` 显示过滤器观察相关报文。抓包结果中可以看到 ICMP Echo Request、ICMP Echo Reply 以及 ARP Request、ARP Reply 等报文。

【插入图 5-3-2：Wireshark 中 ARP 和 ICMP 报文捕获结果】

本次实验中捕获到的典型报文如下表所示。

| 序号 | 协议 | 源地址          | 目的地址        | 报文信息                                    | 作用                                    |
| ---- | ---- | --------------- | --------------- | ------------------------------------------- | --------------------------------------- |
| 8    | ICMP | `10.21.213.119` | `10.21.128.1`   | Echo Request                                | 本机向默认网关发送 ping 请求            |
| 9    | ICMP | `10.21.128.1`   | `10.21.213.119` | Echo Reply                                  | 默认网关向本机返回 ping 响应            |
| 196  | ARP  | `10.21.128.1`   | `10.21.213.119` | Who has `10.21.213.119`? Tell `10.21.128.1` | 默认网关查询本机 IP 地址对应的 MAC 地址 |
| 197  | ARP  | `10.21.213.119` | `10.21.128.1`   | `10.21.213.119` is at `d4:f3:2d:97:c7:4b`   | 本机返回自身 IP 地址对应的 MAC 地址     |

从表中可以看出，本机通过 ICMP 报文完成了与默认网关之间的连通性测试；同时，默认网关也通过 ARP 报文查询并确认了本机 IP 地址 `10.21.213.119` 对应的 MAC 地址。

------

#### 5.3.3 ICMP 报文分析

在 Wireshark 中选中第 8 帧 ICMP Echo Request 报文，并展开 Ethernet II、Internet Protocol Version 4 和 Internet Control Message Protocol 字段，可以看到该报文从本机 `10.21.213.119` 发往默认网关 `10.21.128.1`。

【插入图 5-3-3：ICMP Echo Request 报文详细字段】

本次 ICMP Echo Request 报文的关键字段如下表所示。

| 字段名称        | 实验中观察到的值       | 字段含义                           |
| --------------- | ---------------------- | ---------------------------------- |
| 源 MAC 地址     | `d4:f3:2d:97:c7:4b`    | 本机 WLAN 网卡的 MAC 地址          |
| 目的 MAC 地址   | `10:4f:58:6c:24:00`    | 默认网关对应的 MAC 地址            |
| 源 IP 地址      | `10.21.213.119`        | 本机 IPv4 地址                     |
| 目的 IP 地址    | `10.21.128.1`          | 默认网关 IPv4 地址                 |
| IP 头部长度     | 20 bytes               | IPv4 头部长度                      |
| IP 总长度       | 60 bytes               | 当前 IP 数据报总长度               |
| TTL             | 128                    | 报文生存时间                       |
| 上层协议        | ICMP，协议号为 1       | 表示 IP 数据部分承载的是 ICMP 报文 |
| ICMP 类型       | Echo Request，Type = 8 | 表示该报文为 ping 请求报文         |
| ICMP Code       | 0                      | Echo Request 报文的代码字段        |
| Sequence Number | 5                      | ICMP 请求报文序号                  |
| Data Length     | 32 bytes               | ping 命令默认携带的数据长度        |

随后默认网关 `10.21.128.1` 返回 ICMP Echo Reply 报文，说明目标主机收到请求并成功返回响应。因此，ping 命令中的 4 次请求均得到响应，丢包率为 0%。

本次 ICMP 报文交换过程可以概括为：

```text
10.21.213.119  ->  10.21.128.1    ICMP Echo Request
10.21.128.1    ->  10.21.213.119  ICMP Echo Reply
```

这说明本机与默认网关之间的三层 IP 连通性正常。

------

#### 5.3.4 ARP 报文分析

ARP 协议用于根据 IP 地址查询对应的 MAC 地址。在局域网通信中，主机虽然通过 IP 地址确定通信目标，但实际在以太网链路上传输数据帧时仍然需要使用 MAC 地址。因此，在发送或接收局域网内的 IP 数据报时，主机需要通过 ARP 建立 IP 地址与 MAC 地址之间的映射关系。

本次实验中，在 Wireshark 中捕获到了 ARP Request 和 ARP Reply 报文。其中第 196 帧为 ARP Request，内容为：

```text
Who has 10.21.213.119? Tell 10.21.128.1
```

这表示默认网关 `10.21.128.1` 正在查询谁拥有 IP 地址 `10.21.213.119`。第 197 帧为 ARP Reply，内容为：

```text
10.21.213.119 is at d4:f3:2d:97:c7:4b
```

这表示本机回复默认网关，说明 IP 地址 `10.21.213.119` 对应的 MAC 地址为 `d4:f3:2d:97:c7:4b`。

【插入图 5-3-4：ARP Reply 报文详细字段】

本次 ARP 报文的关键字段如下表所示。

| 序号 | 报文类型    | 源 MAC 地址         | 目的 MAC 地址       | 发送方 IP 地址  | 目标 IP 地址    | 作用                                |
| ---- | ----------- | ------------------- | ------------------- | --------------- | --------------- | ----------------------------------- |
| 196  | ARP Request | `10:4f:58:6c:24:00` | `d4:f3:2d:97:c7:4b` | `10.21.128.1`   | `10.21.213.119` | 默认网关查询本机 IP 对应的 MAC 地址 |
| 197  | ARP Reply   | `d4:f3:2d:97:c7:4b` | `10:4f:58:6c:24:00` | `10.21.213.119` | `10.21.128.1`   | 本机返回自己的 MAC 地址             |

在第 197 帧 ARP Reply 报文中，`Address Resolution Protocol (reply)` 字段显示：

| 字段名称           | 实验中观察到的值    | 字段含义                      |
| ------------------ | ------------------- | ----------------------------- |
| Hardware type      | Ethernet (1)        | 硬件类型为以太网              |
| Protocol type      | IPv4 (0x0800)       | 协议类型为 IPv4               |
| Hardware size      | 6                   | MAC 地址长度为 6 字节         |
| Protocol size      | 4                   | IPv4 地址长度为 4 字节        |
| Opcode             | reply (2)           | 表示该 ARP 报文为响应报文     |
| Sender MAC address | `d4:f3:2d:97:c7:4b` | 发送方，即本机的 MAC 地址     |
| Sender IP address  | `10.21.213.119`     | 发送方，即本机的 IP 地址      |
| Target MAC address | `10:4f:58:6c:24:00` | 目标方，即默认网关的 MAC 地址 |
| Target IP address  | `10.21.128.1`       | 目标方，即默认网关的 IP 地址  |

由此可以看出，ARP 报文完成了 IP 地址到 MAC 地址的映射确认过程，使默认网关能够根据本机的 IP 地址找到对应的链路层 MAC 地址。

------

#### 5.3.5 本节小结

通过本阶段实验，成功捕获并分析了 ARP 和 ICMP 报文。ICMP 部分表明，本机 `10.21.213.119` 能够成功向默认网关 `10.21.128.1` 发送 Echo Request 报文，并收到 Echo Reply 响应，说明本机与默认网关之间的 IP 连通性正常。

ARP 部分表明，默认网关 `10.21.128.1` 通过 ARP Request 查询本机 IP 地址 `10.21.213.119` 对应的 MAC 地址，本机随后通过 ARP Reply 返回自己的 MAC 地址 `d4:f3:2d:97:c7:4b`。该过程说明 ARP 协议在局域网通信中负责完成 IP 地址到 MAC 地址的解析，为后续 IP 数据报在以太网中的传输提供了必要的链路层地址信息。

---

### 5.4 普通 IP 数据分组字段分析

#### 5.4.1 实验操作过程

在完成 ARP 和 ICMP 报文捕获后，本阶段选取前面 ping 默认网关过程中捕获到的一条 ICMP Echo Request 报文，对其封装的 IPv4 数据分组进行分析。

本次选取第 592 帧报文进行分析。该报文为本机 `10.21.213.119` 向默认网关 `10.21.128.1` 发送的 ICMP Echo Request 报文。在 Wireshark 中展开该报文的 `Ethernet II`、`Internet Protocol Version 4` 和 `Internet Control Message Protocol` 字段，可以观察到该报文从链路层到网络层再到 ICMP 协议层的封装结构。

【插入图 5-4-1：普通 IPv4 数据分组字段】

------

#### 5.4.2 IP 数据分组基本结构分析

从 Wireshark 抓包结果可以看到，第 592 帧报文的链路层类型为 `IPv4 (0x0800)`，说明以太网帧的数据部分封装的是 IPv4 数据报。该 IPv4 数据报的源地址为 `10.21.213.119`，目的地址为 `10.21.128.1`，表示该数据报由本机发送到默认网关。

该报文的整体封装关系如下：

```text
Ethernet II
    └── Internet Protocol Version 4
            └── Internet Control Message Protocol
```

其中，`Ethernet II` 负责局域网内的数据帧传输，`Internet Protocol Version 4` 负责网络层的主机到主机传输，`Internet Control Message Protocol` 则是 ping 命令使用的 ICMP 协议。

------

#### 5.4.3 IPv4 关键字段分析表

本次实验选取的 IPv4 数据分组关键字段如下表所示。

| 字段名称                      | 实验中观察到的值       | 字段含义                                         |
| ----------------------------- | ---------------------- | ------------------------------------------------ |
| Frame Number                  | 592                    | 当前数据帧在本次抓包文件中的编号                 |
| Frame Length                  | 74 bytes               | 当前以太网帧的总长度                             |
| Ethernet Type                 | IPv4，`0x0800`         | 表示以太网帧中封装的是 IPv4 数据报               |
| Source MAC Address            | `d4:f3:2d:97:c7:4b`    | 本机 WLAN 网卡的 MAC 地址                        |
| Destination MAC Address       | `10:4f:58:6c:24:00`    | 默认网关对应的 MAC 地址                          |
| IP Version                    | 4                      | 表示该数据分组使用 IPv4 协议                     |
| Header Length                 | 20 bytes               | IPv4 首部长度为 20 字节                          |
| Differentiated Services Field | `0x00`                 | 服务类型字段，本次实验中使用默认值               |
| Total Length                  | 60 bytes               | IPv4 数据报总长度，包括 IP 首部和数据部分        |
| Identification                | `0x97f4 (38900)`       | IP 数据报标识字段，用于分片与重组                |
| Flags                         | `0x0`                  | 标志字段，本报文未设置分片相关标志               |
| Fragment Offset               | 0                      | 分片偏移量为 0，表示该报文不是后续分片           |
| Time to Live                  | 128                    | 生存时间字段，用于限制报文在网络中的最大转发次数 |
| Protocol                      | ICMP，协议号为 1       | 表示 IP 数据部分封装的是 ICMP 报文               |
| Source Address                | `10.21.213.119`        | 发送方 IP 地址，即本机 IPv4 地址                 |
| Destination Address           | `10.21.128.1`          | 接收方 IP 地址，即默认网关 IPv4 地址             |
| ICMP Type                     | Echo Request，Type = 8 | 表示该 ICMP 报文为 ping 请求报文                 |
| ICMP Code                     | 0                      | Echo Request 报文对应的代码字段                  |

------

#### 5.4.4 IP 数据分组作用分析

IPv4 协议位于网络层，主要负责根据源 IP 地址和目的 IP 地址完成主机到主机之间的数据传输。在本次实验中，源 IP 地址为 `10.21.213.119`，目的 IP 地址为 `10.21.128.1`，说明该 IP 数据报是本机发送给默认网关的网络层分组。

该 IPv4 数据报中的 `Protocol` 字段为 `ICMP (1)`，说明 IP 数据部分承载的是 ICMP 报文。进一步展开 ICMP 字段可以看到，该报文类型为 `Echo Request (8)`，即 ping 命令产生的请求报文。因此，该 IPv4 数据报的作用是承载 ICMP Echo Request，从本机发送到默认网关，用于测试本机与默认网关之间的网络连通性。

从链路层字段来看，该报文的源 MAC 地址为 `d4:f3:2d:97:c7:4b`，目的 MAC 地址为 `10:4f:58:6c:24:00`。这说明在局域网实际传输过程中，IP 数据报需要进一步封装在以太网帧中，并通过 MAC 地址完成同一局域网内的链路层传输。

------

#### 5.4.5 分片情况分析

从该 IPv4 数据报的字段可以看出，`Total Length` 为 60 bytes，远小于常见以太网 MTU 1500 字节，因此该 IP 数据报不需要进行分片。

同时，Wireshark 中显示该报文的 `Flags` 字段为 `0x0`，`Fragment Offset` 字段为 0。根据 IPv4 分片机制，如果一个 IP 数据报发生分片，通常会通过 MF 标志位和 Fragment Offset 字段表示分片状态。而本报文的分片偏移量为 0，并且没有设置更多分片标志，说明该报文是一个完整的普通 IPv4 数据报，没有发生 IP 分片。

------

#### 5.4.6 本节小结

通过对第 592 帧 ICMP Echo Request 报文的 IPv4 字段进行分析，可以看出一个普通 IP 数据分组由 IP 首部和数据部分组成。IP 首部中包含版本号、首部长度、总长度、标识字段、标志字段、分片偏移量、TTL、协议号、源 IP 地址和目的 IP 地址等重要信息。

本次实验中，该 IPv4 数据报从本机 `10.21.213.119` 发往默认网关 `10.21.128.1`，其上层协议为 ICMP，用于完成 ping 连通性测试。由于该报文长度较小，且 `Fragment Offset` 为 0，因此没有发生 IP 分片。该实验结果说明，IP 协议负责网络层寻址和分组传输，而 ICMP 报文则作为其数据部分被封装在 IPv4 数据报中进行传输。

---

### 5.5 IP 数据分组的分片传输过程分析

#### 5.5.1 实验操作过程

在前面对普通 IPv4 数据分组进行分析的基础上，本阶段进一步构造长度较大的 IP 数据分组，观察其在传输过程中的分片现象。

首先在 Wireshark 中重新选择 `WLAN` 无线网卡作为捕获接口，并且不设置捕获过滤器，以避免遗漏 IP 分片报文。开始抓包后，在 Wireshark 顶部显示过滤器中输入：

```text
icmp || ip.flags.mf == 1 || ip.frag_offset > 0
```

该过滤器用于同时显示 ICMP 报文和 IPv4 分片报文。其中，`ip.flags.mf == 1` 用于显示设置了 More Fragments 标志的分片，`ip.frag_offset > 0` 用于显示分片偏移量大于 0 的后续分片。

随后在 Windows 命令提示符中执行如下命令：

```bat
ping -4 -l 8000 www.bupt.edu.cn
```

其中，`-4` 表示强制使用 IPv4 协议，`-l 8000` 表示指定 ping 命令携带 8000 字节的数据。命令执行结果显示，`www.bupt.edu.cn` 解析为 `vn46.bupt.edu.cn [10.3.19.2]`，主机共发送 4 个请求报文，收到 4 个响应报文，丢包率为 0%。

【插入图 5-5-1：大包 ping 命令执行结果】

------

#### 5.5.2 IP 分片捕获结果

在 Wireshark 中可以观察到，执行大包 ping 命令后，一个 ICMP Echo Request 被拆分为多个 IPv4 分片进行传输。选取其中一组从本机 `10.21.213.119` 发往目标主机 `10.3.19.2` 的分片进行分析，其 Identification 字段均为 `0xc689`，说明这些分片属于同一个原始 IPv4 数据报。

【插入图 5-5-2：Wireshark 中 IP 分片报文列表】

本次实验中选取的分片组信息如下表所示。

| 分片序号 | 帧号 | 源 IP 地址      | 目的 IP 地址 | Identification | IP Total Length | 数据长度 | MF 标志 | Fragment Offset | 说明         |
| -------- | ---- | --------------- | ------------ | -------------- | --------------- | -------- | ------- | --------------- | ------------ |
| 1        | 3251 | `10.21.213.119` | `10.3.19.2`  | `0xc689`       | 1500            | 1480     | 1       | 0               | 第一个分片   |
| 2        | 3252 | `10.21.213.119` | `10.3.19.2`  | `0xc689`       | 1500            | 1480     | 1       | 1480            | 中间分片     |
| 3        | 3253 | `10.21.213.119` | `10.3.19.2`  | `0xc689`       | 1500            | 1480     | 1       | 2960            | 中间分片     |
| 4        | 3254 | `10.21.213.119` | `10.3.19.2`  | `0xc689`       | 1500            | 1480     | 1       | 4440            | 中间分片     |
| 5        | 3255 | `10.21.213.119` | `10.3.19.2`  | `0xc689`       | 1500            | 1480     | 1       | 5920            | 中间分片     |
| 6        | 3256 | `10.21.213.119` | `10.3.19.2`  | `0xc689`       | 628             | 608      | 0       | 7400            | 最后一个分片 |

从表中可以看出，这 6 个分片具有相同的源 IP 地址、目的 IP 地址、协议类型和 Identification 字段，因此它们属于同一个原始 IP 数据报。前 5 个分片的 IP Total Length 均为 1500 字节，其中 IP 首部长度为 20 字节，数据部分长度为 1480 字节；最后一个分片的 IP Total Length 为 628 字节，数据部分长度为 608 字节。

------

#### 5.5.3 第一个 IP 分片字段分析

选中第 3251 帧并展开 `Internet Protocol Version 4` 字段，可以看到该分片的源 IP 地址为 `10.21.213.119`，目的 IP 地址为 `10.3.19.2`，Identification 字段为 `0xc689 (50825)`。

【插入图 5-5-3：第一个 IP 分片详细字段】

第一个分片的关键字段如下表所示。

| 字段名称            | 实验中观察到的值      | 字段含义                                       |
| ------------------- | --------------------- | ---------------------------------------------- |
| Frame Number        | 3251                  | 当前分片对应的帧号                             |
| Frame Length        | 1514 bytes            | 以太网帧总长度                                 |
| IP Total Length     | 1500 bytes            | 当前 IP 分片总长度                             |
| Identification      | `0xc689 (50825)`      | 标识同一个原始 IP 数据报                       |
| Flags               | `0x1, More fragments` | 表示后面还有更多分片                           |
| More fragments      | Set                   | MF 标志置位，说明不是最后一个分片              |
| Fragment Offset     | 0                     | 分片偏移量为 0，表示该分片从原始数据报开头开始 |
| Protocol            | ICMP (1)              | 该 IP 数据报承载 ICMP 报文                     |
| Source Address      | `10.21.213.119`       | 本机 IP 地址                                   |
| Destination Address | `10.3.19.2`           | 目标主机 IP 地址                               |
| Data                | 1480 bytes            | 当前分片携带的数据长度                         |

由此可见，第 3251 帧是该原始 IP 数据报的第一个分片。其 Fragment Offset 为 0，说明该分片从原始 IP 数据报的数据部分起始位置开始；同时 More fragments 标志被置位，说明该分片后面还有其他分片。

------

#### 5.5.4 最后一个 IP 分片字段分析

选中第 3256 帧并展开 `Internet Protocol Version 4` 字段，可以看到该分片同样具有 Identification `0xc689 (50825)`，说明它与前面若干分片属于同一个原始 IP 数据报。

【插入图 5-5-4：最后一个 IP 分片详细字段】

最后一个分片的关键字段如下表所示。

| 字段名称            | 实验中观察到的值              | 字段含义                                    |
| ------------------- | ----------------------------- | ------------------------------------------- |
| Frame Number        | 3256                          | 当前分片对应的帧号                          |
| Frame Length        | 642 bytes                     | 以太网帧总长度                              |
| IP Total Length     | 628 bytes                     | 当前 IP 分片总长度                          |
| Identification      | `0xc689 (50825)`              | 与前面分片相同，说明属于同一原始 IP 数据报  |
| Flags               | `0x0`                         | 未设置更多分片标志                          |
| More fragments      | Not set                       | MF 标志未置位，说明这是最后一个分片         |
| Fragment Offset     | 7400                          | 当前分片在原始数据报中的偏移位置            |
| Protocol            | ICMP (1)                      | 该 IP 数据报承载 ICMP 报文                  |
| Source Address      | `10.21.213.119`               | 本机 IP 地址                                |
| Destination Address | `10.3.19.2`                   | 目标主机 IP 地址                            |
| Reassembly          | 6 IPv4 Fragments (8008 bytes) | Wireshark 将 6 个分片重组为 8008 字节的数据 |

第 3256 帧的 More fragments 标志未置位，说明该分片后面没有其他分片，因此它是该组 IP 分片中的最后一个分片。同时，其 Fragment Offset 为 7400，说明它位于原始 IP 数据报数据部分的后段。

------

#### 5.5.5 分片结果计算与验证

本次 ping 命令指定数据长度为 8000 字节。ICMP 报文本身还包含 8 字节的 ICMP 首部，因此需要被 IP 层承载的数据总长度为：

```text
8000 + 8 = 8008 bytes
```

从 Wireshark 的重组信息也可以看到：

```text
6 IPv4 Fragments (8008 bytes)
```

前 5 个分片每个携带 1480 字节数据，最后一个分片携带 608 字节数据，因此总数据长度为：

```text
1480 × 5 + 608 = 7400 + 608 = 8008 bytes
```

计算结果与 Wireshark 显示的重组大小一致，说明捕获到的 6 个分片正好组成了完整的 ICMP Echo Request 数据。

由于以太网中常见 MTU 为 1500 字节，而 IPv4 首部长度为 20 字节，因此一个分片最多可以携带：

```text
1500 - 20 = 1480 bytes
```

这也解释了为什么前 5 个分片的 IP Total Length 都为 1500 字节，而最后一个分片由于剩余数据不足 1480 字节，因此 IP Total Length 为 628 字节。

------

#### 5.5.6 本节小结

通过本阶段实验可以看出，当发送的数据长度超过链路层 MTU 时，IPv4 数据报会被拆分为多个分片进行传输。本次实验中，使用 `ping -4 -l 8000 www.bupt.edu.cn` 构造了较大的 ICMP Echo Request 报文，并在 Wireshark 中观察到该报文被拆分为 6 个 IPv4 分片。

这些分片具有相同的源 IP 地址、目的 IP 地址、协议号和 Identification 字段，说明它们属于同一个原始 IPv4 数据报。前 5 个分片的 More fragments 标志均被置位，表示后面还有更多分片；最后一个分片的 More fragments 标志未置位，表示分片传输结束。接收端可以根据 Identification、Fragment Offset 和 More fragments 标志等字段，将这些分片重新组装成完整的 IP 数据报。

本实验直观展示了 IPv4 分片机制的工作过程，加深了对 IP 数据报在不同 MTU 限制下传输方式的理解。

---

### 5.6 TCP 通信过程分析

#### 5.6.1 TCP 流选取

在完成 IP 分片实验后，重新在 Wireshark 中选择 `WLAN` 无线网卡进行抓包，并通过访问 HTTP 网站产生 TCP 连接。为了便于分析同一条 TCP 连接的完整过程，在 Wireshark 中选取一条 TCP 连接并使用 `Follow TCP Stream` 功能进行跟踪，最终得到显示过滤器：

```text
tcp.stream eq 75
```

本次选取的 TCP 流为：

```text
10.21.213.119:33481  <->  23.212.62.78:80
```

其中，`10.21.213.119` 为本机 IP 地址，`33481` 为本机随机分配的临时端口；`23.212.62.78` 为服务器 IP 地址，`80` 为 HTTP 服务端口。

【插入图 5-6-1：TCP Stream 报文列表】

------

#### 5.6.2 TCP 三次握手报文概览

从 `tcp.stream eq 75` 的过滤结果可以看到，该 TCP 连接的前三个报文构成了 TCP 建立连接的三次握手过程。

| 握手次数   | 帧号 | 源 IP:端口            | 目的 IP:端口          | 标志位   | Seq  | Ack  | Len  | 作用                                 |
| ---------- | ---- | --------------------- | --------------------- | -------- | ---- | ---- | ---- | ------------------------------------ |
| 第一次握手 | 1786 | `10.21.213.119:33481` | `23.212.62.78:80`     | SYN      | 0    | 0    | 0    | 客户端请求建立 TCP 连接              |
| 第二次握手 | 1791 | `23.212.62.78:80`     | `10.21.213.119:33481` | SYN, ACK | 0    | 1    | 0    | 服务器确认客户端请求，并同意建立连接 |
| 第三次握手 | 1793 | `10.21.213.119:33481` | `23.212.62.78:80`     | ACK      | 1    | 1    | 0    | 客户端确认服务器响应，TCP 连接建立   |

需要说明的是，Wireshark 默认显示的是相对序列号，因此截图中 Sequence Number 和 Acknowledgment Number 从 0 和 1 开始显示，便于观察 TCP 连接建立过程中的序号变化。

------

#### 5.6.3 第一次握手：客户端发送 SYN 报文

第一次握手对应第 1786 帧报文，方向为：

```text
10.21.213.119:33481 -> 23.212.62.78:80
```

该报文的 TCP 标志位为 `SYN`，表示客户端请求与服务器建立 TCP 连接。此时报文中 `Sequence Number` 为 0，`Acknowledgment Number` 为 0，`TCP Segment Len` 为 0，说明该报文不携带应用层数据，只用于建立连接。

【插入图 5-6-2：第一次握手 SYN 报文详细字段】

第一次握手的关键字段如下表所示。

| 字段名称              | 实验中观察到的值 | 字段含义                   |
| --------------------- | ---------------- | -------------------------- |
| Frame Number          | 1786             | 第一次握手报文对应的帧号   |
| Source IP             | `10.21.213.119`  | 客户端 IP 地址             |
| Destination IP        | `23.212.62.78`   | 服务器 IP 地址             |
| Source Port           | 33481            | 客户端临时端口             |
| Destination Port      | 80               | 服务器 HTTP 端口           |
| Sequence Number       | 0                | 客户端初始相对序列号       |
| Acknowledgment Number | 0                | 此时尚未确认服务器数据     |
| Flags                 | SYN              | 请求建立连接               |
| TCP Segment Len       | 0                | 不携带应用层数据           |
| Window                | 65535            | 客户端接收窗口大小         |
| MSS                   | 1460             | 客户端声明的最大报文段长度 |

该报文说明客户端主动向服务器发起 TCP 连接请求，并告知服务器自己的初始序列号和部分 TCP 选项。

------

#### 5.6.4 第二次握手：服务器发送 SYN, ACK 报文

第二次握手对应第 1791 帧报文，方向为：

```text
23.212.62.78:80 -> 10.21.213.119:33481
```

该报文的 TCP 标志位为 `SYN, ACK`。其中，`SYN` 表示服务器同意建立连接并发送自己的初始序列号，`ACK` 表示服务器已经收到客户端的 SYN 报文。

【插入图 5-6-3：第二次握手 SYN, ACK 报文详细字段】

第二次握手的关键字段如下表所示。

| 字段名称              | 实验中观察到的值 | 字段含义                       |
| --------------------- | ---------------- | ------------------------------ |
| Frame Number          | 1791             | 第二次握手报文对应的帧号       |
| Source IP             | `23.212.62.78`   | 服务器 IP 地址                 |
| Destination IP        | `10.21.213.119`  | 客户端 IP 地址                 |
| Source Port           | 80               | 服务器 HTTP 端口               |
| Destination Port      | 33481            | 客户端临时端口                 |
| Sequence Number       | 0                | 服务器初始相对序列号           |
| Acknowledgment Number | 1                | 确认客户端 SYN，占用一个序列号 |
| Flags                 | SYN, ACK         | 同意建立连接并确认客户端请求   |
| TCP Segment Len       | 0                | 不携带应用层数据               |
| Window                | 64240            | 服务器接收窗口大小             |
| MSS                   | 1386             | 服务器声明的最大报文段长度     |

在该报文中，服务器将确认号设置为 1，表示已经成功接收客户端的 SYN 报文。由于 SYN 标志位会占用一个序列号，所以服务器确认的下一个序列号为 1。

------

#### 5.6.5 第三次握手：客户端发送 ACK 报文

第三次握手对应第 1793 帧报文，方向为：

```text
10.21.213.119:33481 -> 23.212.62.78:80
```

该报文的 TCP 标志位为 `ACK`，表示客户端确认收到了服务器的 SYN, ACK 报文。此时报文中的 `Sequence Number` 为 1，`Acknowledgment Number` 为 1，`TCP Segment Len` 为 0，说明该报文同样不携带应用层数据，只用于完成连接确认。

【插入图 5-6-4：第三次握手 ACK 报文详细字段】

第三次握手的关键字段如下表所示。

| 字段名称              | 实验中观察到的值 | 字段含义                               |
| --------------------- | ---------------- | -------------------------------------- |
| Frame Number          | 1793             | 第三次握手报文对应的帧号               |
| Source IP             | `10.21.213.119`  | 客户端 IP 地址                         |
| Destination IP        | `23.212.62.78`   | 服务器 IP 地址                         |
| Source Port           | 33481            | 客户端临时端口                         |
| Destination Port      | 80               | 服务器 HTTP 端口                       |
| Sequence Number       | 1                | 客户端下一次发送数据时使用的相对序列号 |
| Acknowledgment Number | 1                | 确认服务器 SYN，占用一个序列号         |
| Flags                 | ACK              | 确认服务器响应                         |
| TCP Segment Len       | 0                | 不携带应用层数据                       |
| Window                | 65280            | 客户端当前接收窗口大小                 |

第三次握手完成后，客户端和服务器双方都确认了对方的发送和接收能力，TCP 连接正式建立。随后，客户端即可在该连接上发送 HTTP 请求数据。

------

#### 5.6.6 TCP 三次握手过程小结

通过对第 1786、1791 和 1793 帧报文的分析，可以清楚看到 TCP 连接建立过程中的三次握手。

第一次握手中，客户端 `10.21.213.119:33481` 向服务器 `23.212.62.78:80` 发送 SYN 报文，请求建立连接。第二次握手中，服务器返回 SYN, ACK 报文，一方面确认客户端的连接请求，另一方面发送自己的连接建立请求。第三次握手中，客户端返回 ACK 报文，确认服务器的响应。至此，双方完成连接建立，可以开始后续的数据传输。

该过程说明，TCP 在传输应用层数据之前，需要先通过三次握手确认通信双方的收发能力，并同步双方的初始序列号。三次握手完成后，TCP 才进入数据传输阶段。

---

#### 5.6.7 TCP 数据通信过程分析

在完成 TCP 三次握手后，客户端和服务器之间的 TCP 连接已经建立，随后开始在该连接上传输应用层数据。本次实验中，继续使用前面选取的 TCP 流：

```text
tcp.stream eq 75
```

该 TCP 连接为：

```text
10.21.213.119:33481  <->  23.212.62.78:80
```

其中，`10.21.213.119:33481` 为客户端，即本机；`23.212.62.78:80` 为服务器端，端口 80 表示该连接承载的是 HTTP 通信。

【插入图 5-6-5：TCP 数据通信报文列表】

------

#### 5.6.8 TCP 数据通信报文概览

从 Wireshark 中 `tcp.stream eq 75` 的过滤结果可以看到，在三次握手完成后，客户端向服务器发送 HTTP GET 请求，服务器确认收到请求后返回 HTTP/1.1 200 OK 响应，随后客户端再对服务器返回的数据进行确认。

本次实验中 TCP 数据通信阶段的典型报文如下表所示。

| 帧号 | 方向                                     | 协议 / 标志位              | Seq  | Ack  | Len  | 作用                           |
| ---- | ---------------------------------------- | -------------------------- | ---- | ---- | ---- | ------------------------------ |
| 1795 | `10.21.213.119:33481 -> 23.212.62.78:80` | HTTP GET / PSH, ACK        | 1    | 1    | 111  | 客户端发送 HTTP GET 请求       |
| 1803 | `23.212.62.78:80 -> 10.21.213.119:33481` | ACK                        | 1    | 112  | 0    | 服务器确认收到客户端请求       |
| 1805 | `23.212.62.78:80 -> 10.21.213.119:33481` | HTTP/1.1 200 OK / PSH, ACK | 1    | 112  | 187  | 服务器返回 HTTP 响应数据       |
| 1809 | `10.21.213.119:33481 -> 23.212.62.78:80` | ACK                        | 112  | 189  | 0    | 客户端确认收到服务器返回的数据 |

从表中可以看出，TCP 数据通信过程中，发送方每发送一段数据，接收方都会通过 ACK 报文进行确认。Sequence Number 和 Acknowledgment Number 的变化反映了 TCP 按字节编号和可靠确认的传输机制。

------

#### 5.6.9 客户端发送 HTTP GET 请求

第 1795 帧为客户端发送给服务器的 HTTP 请求报文，方向为：

```text
10.21.213.119:33481 -> 23.212.62.78:80
```

在该报文中，TCP 字段显示 `Seq = 1`，`Ack = 1`，`Len = 111`，说明客户端从相对序列号 1 开始，向服务器发送了 111 字节的应用层数据。该报文的 TCP 标志位为 `PSH, ACK`，其中 ACK 表示该报文仍然确认前面服务器的序列号，PSH 表示发送端希望接收端尽快将数据交付给上层应用程序。

【插入图 5-6-6：客户端 HTTP GET 请求报文详细字段】

在 Hypertext Transfer Protocol 字段中可以看到，请求行为：

```text
GET /connecttest.txt HTTP/1.1
```

同时还可以看到请求头中包含：

```text
Connection: Close
User-Agent: Microsoft NCSI
Host: www.msftconnecttest.com
```

这说明客户端通过已经建立的 TCP 连接向服务器发送了一个 HTTP GET 请求，请求访问 `/connecttest.txt` 资源。

------

#### 5.6.10 服务器确认客户端请求

第 1803 帧为服务器发送给客户端的 TCP ACK 报文，方向为：

```text
23.212.62.78:80 -> 10.21.213.119:33481
```

该报文中 `Seq = 1`，`Ack = 112`，`Len = 0`。由于客户端在第 1795 帧中从 `Seq = 1` 开始发送了 111 字节的数据，因此服务器返回的确认号为：

```text
1 + 111 = 112
```

这表示服务器已经成功接收客户端发送的 111 字节 HTTP GET 请求，并期望客户端下一次从序列号 112 开始继续发送数据。该 ACK 报文自身不携带应用层数据，因此 `Len = 0`。

这一过程体现了 TCP 的可靠确认机制：接收方通过确认号告诉发送方已经成功接收到的数据范围。

------

#### 5.6.11 服务器返回 HTTP 200 OK 响应

第 1805 帧为服务器返回给客户端的 HTTP 响应报文，方向为：

```text
23.212.62.78:80 -> 10.21.213.119:33481
```

该报文的 TCP 字段显示 `Seq = 1`，`Ack = 112`，`Len = 187`，说明服务器从自己的相对序列号 1 开始，向客户端发送了 187 字节的响应数据，同时继续确认已经收到客户端序列号 112 之前的数据。

【插入图 5-6-7：服务器 HTTP/1.1 200 OK 响应报文详细字段】

在 Hypertext Transfer Protocol 字段中可以看到，服务器返回的状态行为：

```text
HTTP/1.1 200 OK
```

同时还可以看到部分响应头字段：

```text
Content-Length: 22
Connection: close
Content-Type: text/plain
```

其中，`HTTP/1.1 200 OK` 表示服务器成功处理了客户端请求；`Content-Type: text/plain` 表示返回内容为纯文本；`Connection: close` 表示服务器在完成本次响应后将关闭连接。

------

#### 5.6.12 客户端确认服务器响应

服务器发送 HTTP 响应后，客户端返回 ACK 报文进行确认。在第 1809 帧中，客户端向服务器发送 ACK 报文，方向为：

```text
10.21.213.119:33481 -> 23.212.62.78:80
```

该报文中 `Seq = 112`，`Ack = 189`，`Len = 0`。其中，`Seq = 112` 表示客户端下一次发送数据时将从序列号 112 开始；`Ack = 189` 表示客户端已经确认收到服务器发送的数据，并期待服务器下一次从序列号 189 开始继续发送。

由于第 1807 帧中服务器已经发送了 `FIN, ACK` 报文，因此第 1809 帧的确认号同时包含了对服务器数据和关闭请求的确认。连接关闭过程将在后续 TCP 连接拆除部分中继续分析。

------

#### 5.6.13 TCP 数据通信过程小结

通过对第 1795、1803、1805 和 1809 帧报文的分析，可以看到 TCP 数据通信过程中的可靠传输机制。

客户端首先在已经建立的 TCP 连接上发送 HTTP GET 请求，请求长度为 111 字节。服务器收到后返回 ACK 报文，并将确认号设置为 112，表示已经成功收到客户端从序列号 1 开始的 111 字节数据。随后服务器返回 HTTP/1.1 200 OK 响应，响应数据长度为 187 字节。客户端收到响应后再次发送 ACK 进行确认。

该过程说明，TCP 并不是简单地发送数据，而是通过 Sequence Number 和 Acknowledgment Number 对字节流进行编号和确认，从而保证数据能够按序、可靠地传输到对方。应用层的 HTTP 请求和响应正是通过 TCP 连接承载并完成传输的。

---

#### 5.6.14 TCP 连接拆除过程分析

在完成 HTTP 请求与响应的数据传输后，TCP 连接需要被正常释放。本次实验继续使用前面选取的同一条 TCP 流：

```text
tcp.stream eq 75
```

该连接对应的通信双方为：

```text
10.21.213.119:33481  <->  23.212.62.78:80
```

其中，`10.21.213.119:33481` 为客户端，即本机；`23.212.62.78:80` 为服务器端。通过 Wireshark 抓包结果可以看到，该 TCP 连接在数据传输结束后，通过 FIN 和 ACK 报文完成了连接的优雅拆除。

【插入图 5-6-8：TCP 连接拆除报文列表】

------

#### 5.6.15 TCP 连接拆除报文概览

从 `tcp.stream eq 75` 的过滤结果可以看到，本次 TCP 连接由服务器端首先发起关闭。服务器发送 `FIN, ACK` 报文后，客户端先返回 ACK 进行确认；随后客户端也发送 `FIN, ACK` 报文，请求关闭自己这一方向的连接；最后服务器返回 ACK，连接释放完成。

本次实验中 TCP 连接拆除过程的关键报文如下表所示。

| 步骤       | 帧号 | 方向                                     | 标志位   | Seq  | Ack  | Len  | 作用                       |
| ---------- | ---- | ---------------------------------------- | -------- | ---- | ---- | ---- | -------------------------- |
| 第一次挥手 | 1807 | `23.212.62.78:80 -> 10.21.213.119:33481` | FIN, ACK | 188  | 112  | 0    | 服务器请求关闭连接         |
| 第二次挥手 | 1809 | `10.21.213.119:33481 -> 23.212.62.78:80` | ACK      | 112  | 189  | 0    | 客户端确认服务器的关闭请求 |
| 第三次挥手 | 1811 | `10.21.213.119:33481 -> 23.212.62.78:80` | FIN, ACK | 112  | 189  | 0    | 客户端也请求关闭连接       |
| 第四次挥手 | 1835 | `23.212.62.78:80 -> 10.21.213.119:33481` | ACK      | 189  | 113  | 0    | 服务器最终确认，连接释放   |

从表中可以看出，该 TCP 连接的拆除过程不是客户端先关闭，而是服务器在返回 HTTP 响应后首先发起关闭连接。这与 HTTP 响应头中的 `Connection: close` 字段相对应，说明服务器在完成响应后主动关闭连接。

------

#### 5.6.16 第一次挥手：服务器发送 FIN, ACK 报文

第 1807 帧为服务器发送给客户端的 `FIN, ACK` 报文，方向为：

```text
23.212.62.78:80 -> 10.21.213.119:33481
```

该报文的 `Sequence Number` 为 188，`Acknowledgment Number` 为 112，`TCP Segment Len` 为 0，说明该报文不携带应用层数据，只用于连接关闭控制。其中，FIN 标志位表示服务器端已经没有数据需要继续发送，请求关闭服务器到客户端方向的连接；ACK 标志位表示服务器仍然确认已经收到客户端前面发送的数据。

【插入图 5-6-9：服务器发送 FIN, ACK 报文详细字段】

第 1807 帧关键字段如下表所示。

| 字段名称              | 实验中观察到的值 | 字段含义                              |
| --------------------- | ---------------- | ------------------------------------- |
| Frame Number          | 1807             | 第一次挥手报文对应的帧号              |
| Source IP             | `23.212.62.78`   | 服务器 IP 地址                        |
| Destination IP        | `10.21.213.119`  | 客户端 IP 地址                        |
| Source Port           | 80               | 服务器 HTTP 端口                      |
| Destination Port      | 33481            | 客户端临时端口                        |
| Sequence Number       | 188              | 服务器当前相对序列号                  |
| Acknowledgment Number | 112              | 确认客户端序列号 112 之前的数据已收到 |
| Flags                 | FIN, ACK         | 请求关闭连接并确认客户端数据          |
| TCP Segment Len       | 0                | 不携带应用层数据                      |

------

#### 5.6.17 第二次挥手：客户端返回 ACK 报文

第 1809 帧为客户端发送给服务器的 ACK 报文，方向为：

```text
10.21.213.119:33481 -> 23.212.62.78:80
```

该报文的 `Sequence Number` 为 112，`Acknowledgment Number` 为 189，`TCP Segment Len` 为 0。由于第 1807 帧中服务器发送的 FIN 报文序列号为 188，而 FIN 标志位会占用一个序列号，因此客户端返回确认号：

```text
188 + 1 = 189
```

这表示客户端已经收到服务器发送的 FIN, ACK 报文，并确认服务器到客户端方向的连接关闭请求。

【插入图 5-6-10：客户端确认服务器关闭请求的 ACK 报文，可选】

该报文只设置 ACK 标志位，不携带应用层数据，因此它的主要作用是确认服务器端的关闭请求。

------

#### 5.6.18 第三次挥手：客户端发送 FIN, ACK 报文

第 1811 帧为客户端发送给服务器的 `FIN, ACK` 报文，方向为：

```text
10.21.213.119:33481 -> 23.212.62.78:80
```

该报文的 `Sequence Number` 为 112，`Acknowledgment Number` 为 189，`TCP Segment Len` 为 0。FIN 标志位表示客户端也已经没有数据需要继续发送，请求关闭客户端到服务器方向的连接；ACK 标志位继续确认服务器端前面的数据和关闭请求。

【插入图 5-6-11：客户端发送 FIN, ACK 报文详细字段】

第 1811 帧关键字段如下表所示。

| 字段名称              | 实验中观察到的值 | 字段含义                              |
| --------------------- | ---------------- | ------------------------------------- |
| Frame Number          | 1811             | 第三次挥手报文对应的帧号              |
| Source IP             | `10.21.213.119`  | 客户端 IP 地址                        |
| Destination IP        | `23.212.62.78`   | 服务器 IP 地址                        |
| Source Port           | 33481            | 客户端临时端口                        |
| Destination Port      | 80               | 服务器 HTTP 端口                      |
| Sequence Number       | 112              | 客户端当前相对序列号                  |
| Acknowledgment Number | 189              | 确认服务器序列号 189 之前的数据已收到 |
| Flags                 | FIN, ACK         | 客户端请求关闭连接并确认服务器数据    |
| TCP Segment Len       | 0                | 不携带应用层数据                      |

------

#### 5.6.19 第四次挥手：服务器返回最终 ACK 报文

第 1835 帧为服务器发送给客户端的最终 ACK 报文，方向为：

```text
23.212.62.78:80 -> 10.21.213.119:33481
```

该报文的 `Sequence Number` 为 189，`Acknowledgment Number` 为 113，`TCP Segment Len` 为 0。由于第 1811 帧中客户端发送的 FIN 报文序列号为 112，而 FIN 标志位同样会占用一个序列号，因此服务器返回确认号：

```text
112 + 1 = 113
```

这表示服务器已经收到客户端的 FIN, ACK 报文，并确认客户端到服务器方向的关闭请求。至此，通信双方均完成关闭确认，该 TCP 连接释放完成。

【插入图 5-6-12：服务器最终确认 ACK 报文详细字段】

第 1835 帧关键字段如下表所示。

| 字段名称              | 实验中观察到的值 | 字段含义                       |
| --------------------- | ---------------- | ------------------------------ |
| Frame Number          | 1835             | 第四次挥手报文对应的帧号       |
| Source IP             | `23.212.62.78`   | 服务器 IP 地址                 |
| Destination IP        | `10.21.213.119`  | 客户端 IP 地址                 |
| Source Port           | 80               | 服务器 HTTP 端口               |
| Destination Port      | 33481            | 客户端临时端口                 |
| Sequence Number       | 189              | 服务器当前相对序列号           |
| Acknowledgment Number | 113              | 确认客户端 FIN，占用一个序列号 |
| Flags                 | ACK              | 最终确认客户端关闭请求         |
| TCP Segment Len       | 0                | 不携带应用层数据               |

------

#### 5.6.20 TCP 连接拆除过程小结

通过对第 1807、1809、1811 和 1835 帧报文的分析，可以看到本次 TCP 连接采用了较为典型的四次挥手机制完成连接释放。

首先，服务器 `23.212.62.78:80` 在完成 HTTP 响应后，向客户端发送 `FIN, ACK` 报文，请求关闭服务器到客户端方向的连接。客户端收到后返回 ACK 报文进行确认。随后，客户端 `10.21.213.119:33481` 也发送 `FIN, ACK` 报文，请求关闭客户端到服务器方向的连接。最后，服务器返回 ACK 报文确认客户端的关闭请求，至此 TCP 连接正式释放。

在该过程中可以观察到，FIN 报文虽然不携带应用层数据，但会占用一个序列号。因此服务器发送 `Seq = 188` 的 FIN 后，客户端返回 `Ack = 189`；客户端发送 `Seq = 112` 的 FIN 后，服务器返回 `Ack = 113`。这说明 TCP 在连接拆除阶段仍然使用序列号和确认号机制，保证连接关闭过程可靠、有序地完成。
